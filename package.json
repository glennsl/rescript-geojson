{
  "scripts": {
    "build": "rescript build -with-deps",
    "start": "rescript build -w -with-deps",
    "clean": "rescript clean",
    "fmt": "rescript format -all",
    "test": "npm run build && node lib/js/test/tests.js"
  },
  "dependencies": {
    "@glennsl/rescript-json-combinators": "^1.2.1",
    "rescript": "^10.1.4"
  }
}
